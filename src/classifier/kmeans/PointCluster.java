/**
 * PointCluster.java
 * @author H. Ripoche
 * 15/02/2002
 */
/*
import java.util.Vector;

public class PointCluster extends Cluster {

  public PointCluster () {
    super();
  };

  Instance createCentroid() {
    double x = 0, y = 0;
    Instance inst;
    Point p;
    for (int i=0; i<this.size(); i++) {
      inst = (Instance) this.elementAt(i);
      p = (Point) inst;
      x += p.getX();
      y += p.getY();
    }
    Point result = new Point(x,y);
    return result;
  }
}
*/
